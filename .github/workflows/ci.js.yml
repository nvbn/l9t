name: ci

on: [push, pull_request]

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macOS-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - run: curl -fsSL https://deno.land/x/install/install.sh | sh
      - run: ~/.deno/bin/deno test
